Answers : 

1) i) a is a number or string
   ii)a is a variable

2) i,j = j,i

3) puts "No tweets for you !" unless tweets.empty? 
      
   else
  	tweets = Timeline.new("Horrah ! finally I am on tweeter .") 
	puts tweets
   end
